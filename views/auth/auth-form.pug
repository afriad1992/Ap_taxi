extends ../layout
block content
 nav
  div(class="nav-wrapper white" style="height:70px !important")
    a(href="/" class="brand-logo")
      img( src="/images/Capture.PNG" height="70")
    ul(id="nav-mobile" class="right hide-on-med-and-down")  
     li
      a(style="color:black" class="waves-effect waves-light btn  amber darken-3" href="/") Inscription 
     li
      a(style="color:black"  href="#"  class="waves-effect waves-light btn  amber darken-3") Connexion 
 .container 
   div(class="row")
   div(class="row")
   div(class="row")
   div(class="row col s4 offset-m5" style="position:relative,bottom:20px")
    form(id="docs" class="card w-50 mt-5" method="post" action="/auth/signin" style="position:relative,top:20px" )
      div(class="card-title") Connexion  
      div(class="card-content")
       div(class="row")
        
        div(class="input-field col s12")
          i(class="material-icons prefix") contact_mail
          input(placeholder="Placeholder" id="mail" type="email" class="validate" name="email" v-model="nom" v-on:blur="verifnom()" )
          label  email
          span(style="color:red;")
        div(class="input-field col s12")
          i(class="material-icons prefix") lock
          input(placeholder="Placeholder" id="passe2" type="password" class="validate" name="password" v-model="nom" v-on:blur="verifnom()" )
          label Mot de passe 
          span(style="color:red;") 
               
        if errors
          ul(class="my-2")
            each error in errors
              li(class="text-danger")= error
        div(class="w-100")
          input(type="submit" value="se connecter" class="btn btn-large col s4 offset-s4 ml-auto d-block")